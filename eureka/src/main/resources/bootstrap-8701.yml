spring:
  application:
    name: Eureka

server:
  port: 8701

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8702/eureka
  server:
    enable-self-preservation: false
