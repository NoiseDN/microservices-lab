spring:
  application:
    name: Dashboard

server:
  port: 8000

turbine:
  aggregator:
    clusterConfig: BILL,GUEST,ORDER,STOCK,TABLE
  appConfig: Bill,Guest,Order,Stock,Table

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8701/eureka,http://localhost:8702/eureka